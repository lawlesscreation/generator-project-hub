language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-project-hub' ]; then cd .. && eval "mv $currentfolder generator-project-hub" && cd generator-project-hub; fi

